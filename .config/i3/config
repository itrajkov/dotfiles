# vim: filetype=i3config
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: "Hack Nerd Font" FontAwesome 10
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+Shift+q kill

bindsym $mod+Shift+BackSpace focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, focus parent, kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+z split h
# split in vertical orientation
bindsym $mod+x split v 
# enter fullscreen mode for the focused container 
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split) bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+q layout stacking

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $workspace1 1
set $workspace2 2
set $workspace3 3
set $workspace4 4
set $workspace5 5
set $workspace6 6
set $workspace7 7
set $workspace8 8
set $workspace9 9
set $workspace10 10

# switch to workspace
bindcode $mod+10 workspace $workspace1
bindsym $mod+7 workspace $workspace2
bindsym $mod+5 workspace $workspace3
bindsym $mod+3 workspace $workspace4
bindsym $mod+c workspace $workspace6
bindsym $mod+s workspace $workspace7

# move focused container to workspace
bindcode $mod+Shift+10 move container to workspace $workspace1
bindsym $mod+Shift+7 move container to workspace $workspace2
bindsym $mod+Shift+5 move container to workspace $workspace3
bindsym $mod+Shift+3 move container to workspace $workspace4
bindsym $mod+Shift+c move container to workspace $workspace6
bindsym $mod+Shift+s move container to workspace $workspace7

# enables back and forth
workspace_auto_back_and_forth yes

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Exit?' -b 'Yes!' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set $bg-color           #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935

# COLORS

client.focused          #2e7677 #2e7677 #F8F8F2 #2e7677   #2e7677
client.focused_inactive #839496 #44475A #F8F8F2 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #073642
client.urgent           #282A36 #859900 #FFFFFF #859900   #859900 
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36
client.background       #002b36 

# BAR CONFIG
# bar {
#     font pango: FontAwesome 13
#     position top
#     status_command i3blocks -c ~/.config/i3/i3blocks.conf
#     colors {
#     background #073F42 
#     statusline #F8F8F2
#     separator  #44475A

#     focused_workspace  #073642 #2aa198 #eee8d5
#     active_workspace   #282A36 #44475A #eee8d5
#     inactive_workspace #073642 #073642 #eee8d5
#     urgent_workspace  #282A36 #859900 #FFFFFF
#    binding_mode       #FF5555 #FF5555 #F8F8F2
#   }
# }

## i3-gaps CONGFIG ##
for_window [class=".*"] border pixel 2
gaps inner 5
gaps horizontal 8
gaps top 8
gaps bottom 8
# smart_gaps on
smart_borders on

# Rounded corners
#border_radius 12

#Rofi binds
bindsym $mod+d exec rofi -show drun -show-icons -font "Hack Nerd Font 16"
bindsym $mod+Shift+d exec rofi -show emoji -modi emoji
bindsym $mod+g exec rofi -dmenu -p "Google" | xargs -I{} xdg-open https://www.google.de/search?q={}
bindsym $mod+y exec rofi -dmenu -p "Youtube" | xargs -I{} xdg-open https://www.youtube.com/results?search_query={}

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 1 toggle

bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5 # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5 # decrease screen brightness

# SESSION CONTROLS
bindsym F9 exec i3lock
bindsym $mod+Shift+F13 exec xfce4-session-logoutdi

# Media player controls
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Screenshots
bindsym --release Print exec --no-startup-id maim -s -u | xclip -selection clipboard -t image/png -i
bindsym Shift+Print exec --no-startup-id scrot '%Y-%m-%d_%T_$wx$h.png' -e 'mv $f ~/Pictures/screenshots'

# Keyboard
exec_always setxkbmap -layout dvp,us,mk
exec_always setxkbmap -option 'grp:alt_shift_toggle'

# Disable PC Speaker
exec_always xset -b

#run nm applet and bluetooth applet
exec nm-applet
exec blueman-applet

# Picom compositor
exec_always picom --config ~/.config/picom/picom.conf

# Wallpaper
exec_always feh --bg-fill ~/.config/i3/wallpaper.png

# Monitor binds
bindcode 87 exec xrandr --output HDMI1 --off && i3-msg restart
bindcode 88 exec xrandr --output HDMI1 --auto --right-of eDP1 && i3-msg restart 

# Custom binds
bindsym $mod+m exec kitty spt
bindsym $mod+shift+x exec alacritty -e ranger
bindsym $mod+shift+a exec ~/.scripts/lazyweeb.sh
bindsym $mod+shift+f exec ~/.scripts/pdf-opener.sh
bindsym $mod+shift+p exec rofi-pass
bindsym $mod+shift+b exec qutebrowser

bindsym $mod+shift+n exec emacsclient -c
bindsym $mod+shift+m exec emacsclient --eval '(mu4e)' -c
bindsym $mod+n exec emacsclient -c ~/Dropbox/org/ivches-system/dashboard.org

# Startup scripts
exec xmodmap ~/.Xmodmap
exec ~/.scripts/battery-notif.sh
exec_always ~/.config/polybar/launch.sh 

# Daemons
exec redshift -l 41.99646:21.43141
exec xautolock -locker ~/.scripts/lock.sh -time 10 -bell 0 -corners +00-
exec pcmanfm -d
exec lxpolkit
exec spotifyd

# Find my phone
bindsym $mod+shift+0 exec kdeconnect-cli --ring -d 5d992100d51da880 

# TEMP
for_window [class="Conway's gaem of lif"] floating enable
for_window [class=".*"] move position center
