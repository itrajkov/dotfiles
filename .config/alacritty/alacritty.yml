shell:
  program: /bin/zsh
  args:
    - -l
    - -c
    - "tmux attach || tmux"
    
import:
  - ~/.config/alacritty/dracula.yml

# Font configuration
font:
  normal:
    family: Hack Nerd Font
  size: 14

env:
  TERM: xterm-256color

background_opacity: 0.8
  
window:
  padding:
   x: 10
   y: 10
